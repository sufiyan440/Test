pipeline {
    agent any

    stages {
        stage('git checkout') {
            steps {  
            git branch: 'master', credentialsId: 'Gitcrd', url: 'https://github.com/sufiyan440/Test.git'
            }
        }
    }
}
